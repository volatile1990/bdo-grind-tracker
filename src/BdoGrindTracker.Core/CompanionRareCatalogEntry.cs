// Restored from the verified 0.5.1 assembly; recognition behavior is intentionally unchanged.
namespace BdoGrindTracker.Core;

public sealed record CompanionRareCatalogEntry(string Name, string? IconPath = null);

